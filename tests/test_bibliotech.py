"""
Adicione aqui os testes de caixa preta e caixa branca da sua equipe.

Sugestão de organização (pode dividir em vários arquivos se preferir):
    tests/test_pode_emprestar.py
    tests/test_calcular_multa.py
    tests/test_classificar_atraso.py

Lembre-se:
- Etapa 1 (caixa preta): baseie-se apenas em `requisitos.md`.
- Etapa 2 (caixa branca): depois de liberado pelo professor, abra
  `src/bibliotech.py` e analise decisões, condições e caminhos.

Exemplo de esqueleto:

    from src.bibliotech import pode_emprestar

    def test_usuario_valido_pode_emprestar():
        resultado = pode_emprestar(True, False, 0)
        assert resultado is True
"""
